<%= form_with(model: question) do |form| %>
<div class="mb-3">
  <%= form.label :title, "タイトル（20字以内）", class: "form-label", for: "validationServer03" %>
  <%= form.text_field :title, class: "form-control form-control-lg" %>
</div>
<div class="mb-3">
  <%= form.label :body, "内容（250字以内）", class: "form-label" %>
  <%= form.text_area :body, class: "form-control form-control-lg", rows: "5" %>
</div>
  <div class="d-grid gap-2 col-lg mx-auto mb-3">
    <%= form.submit "投稿", class: "btn btn-success" %>
  </div>
<% end %>