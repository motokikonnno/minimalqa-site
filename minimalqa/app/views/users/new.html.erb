<body class="text-center pt-5">
<main class="form-signin">
      <h1 class="h3 mb-5 fw-normal mt-5">新規登録</h1>
      <% if @user.errors.any? %>
      <div class="text-danger mb-4">
          ※ 空、もしくは指定の文字数を超えています。
      </div>
      <% end %>
      <div class="card" style="width: 18rem;">
        <div class="card-body">
      <%= form_with(model: @user, local: true) do |f| %> 
      <div class="form-floating">
        <%= f.text_field :name, class: "form-control", placeholder: "名前(8文字以内)" %>
        <%= f.label :name, "名前(8文字以内)", class: "text-black-50", for: "floatingInputInvalid" %>
      </div>
      <div class="form-floating">
        <%= f.password_field :password, class: "form-control", placeholder: "パスワード" %>
        <%= f.label :password, "パスワード", class: "text-black-50", for: "floatingInput" %>
      </div>
      <div class="form-floating">
        <%= f.password_field :password_confirmation, class: "form-control", placeholder: "パスワード確認" %>
        <%= f.label :password_confirmation, "パスワード確認", class: "text-black-50", for: "floatingInput" %>
      </div>
      <%= f.submit "新規登録", class: "w-100 btn btn-lg btn-success mt-5" %>
      <% end %>
      </div>
      </div>
</main>
<div class="gap-2 col-3 mx-auto mt-5">
もしくは
<%= link_to("ログイン", new_session_path, {class: "btn btn-success text-light"}) %>
</div>
</body>